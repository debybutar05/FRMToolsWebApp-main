{
  "name": "server",
  "version": "0.0.1",
  "scripts": {
    "start:dev": "nest start --watch",
    "build": "nest build",
    "build:client": "npm --prefix ./client run build",
    "start": "node dist/main.js",
    "start:all": "npm run build:client && nest start --watch"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/serve-static": "^4.0.2",
    "@nestjs/typeorm": "^10.0.2",
    "express": "^4.21.2",
    "path": "^0.12.7",
    "pg": "^8.16.3",
    "typeorm": "^0.3.26"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/serve-static": "^1.15.8",
    "concurrently": "^9.2.1",
    "typescript": "^5.2.0"
  }
}
